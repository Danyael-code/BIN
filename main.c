#include <stdio.h>

int main()
{
    //Var Def
    int length, width, area;
    
    //Var Init
    printf("Calculate Area of Rectangle");
    length = 5;
    width = 3;
    area = length*width;
    printf("Area of Rectangle = %d \n", area);
    
    return 0;
}